# 2026-07-29 更新记录

## 目标

在 `mytest-test-two.myshopify.com` 创建可按产品标签自动归类的海报产品系列，并发布到默认销售渠道。

## 后台调整

- 新建 `Movie Posters`：标签包含 `Movie`，当前命中 56 件产品。
- 新建 `Music Posters`：标签包含 `Music`，当前命中 28 件产品。
- 新建 `Anime Posters`：标签包含 `Anime`，当前命中 9 件产品。
- 新建 `Horror Posters`：标签包含 `Horror`，当前命中 5 件产品。
- 新建 `Space Posters`：标签包含 `Space`，当前命中 1 件产品。
- 新建 `TV Series Posters`：标签包含 `TV Series`，当前命中 10 件产品。

## 发布与自检

- 每个系列均为自动系列，规则为“标签包含对应标签”。
- 创建页均显示“已发布到 3 个渠道”，其中包含 Online Store。
- 已在 Shopify 产品系列列表中逐项确认六个系列和对应命中数量。

## 影响范围

- 仅新增 Shopify 后台产品系列；未改动主题代码、产品数据或页面样式。
