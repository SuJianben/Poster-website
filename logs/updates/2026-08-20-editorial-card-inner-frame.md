# 2026-08-20 首页 Editorial 商品卡单内框

## 本次目标

- 移除商品图片卡最外层的粗黑框。
- 仅保留一条克制的 1px 内框，让图片留白仍有清晰边界。
- 保留商品图片留白、Hover 第二张图、阴影、倾斜和下方文案布局。

## 修改范围

- `assets/editorial-depth-cards.css`
- `tests/editorial-product-card-layout.test.ps1`

## 调整内容

- 默认态移除 5px 深色内嵌边框和叠加白边，改为单条 1px 半透明内框。
- Hover / 键盘聚焦态同步使用同一内框，避免交互时粗黑框重新出现。
- 适度降低默认投影强度，避免去框后阴影显得过重。

## 影响范围

- 仅影响首页 Editorial 商品卡的图片卡边缘。
- 不影响商品数据、图片顺序、留白比例、Hover 图片来源或其他商品模块。

## 自检

- 状态：已完成，并已同步到正式主题。
- Shopify Theme Check：共检查 62 个主题文件，0 个问题。
- 自动化测试：Editorial 商品卡专项测试通过，确认粗黑框不会被重新加入。
- 桌面端：正式页面共识别 8 张商品卡；计算样式仅保留 1 层 `1px` 内框，原 `5px` 粗黑内框已移除。
- Hover：悬停态仍使用同一条 `1px` 内框，没有重新出现粗黑框。
- 移动端：在 `390 × 844 px` 视口下保持两列，左右留白约 `14.7px`、上下约 `22.06px`，无横向溢出。
- 浏览器控制台：0 条错误或警告。

## 遗留问题

- 无。
