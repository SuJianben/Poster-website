# 2026-08-20 首页 Editorial 商品卡 Hover 与折扣角标

## 本次目标

- 第一张作品图继续以留白方式完整展示。
- Hover 第二张图铺满卡片，作为场景预览。
- 内框颜色统一为 `#ebebeb`。
- 默认态不显示外投影，Hover 时淡入轻投影，移开后平滑淡出。
- 左上角增加 `#8dc29c` 折扣角标。

## 修改范围

- `sections/editorial-with-products.liquid`
- `assets/editorial-depth-cards.css`
- `tests/editorial-product-card-layout.test.ps1`

## 新增内容

- 折扣角标在商品有 `compare_at_price` 时自动计算并显示真实折扣百分比。
- 尚未设置划线价时使用主题编辑器中的正式备用文案，默认显示 `Discount`，避免展示虚假的折扣数字。

## 调整内容

- 第二张 Hover 图移出第一张图的留白容器，改为覆盖完整卡片。
- 默认态仅保留 `#ebebeb` 的 1px 内框，不显示外投影。
- Hover 增加轻量投影，并为进入和离开设置平滑过渡。

## 影响范围

- 仅影响首页 Editorial 商品卡。
- 不修改商品价格、划线价、媒体顺序、商品详情页或其他首页模块。

## 自检

- 状态：已完成，并已同步到正式主题。
- Shopify Theme Check：共检查 62 个主题文件，0 个问题。
- 自动化测试：Editorial 商品卡专项测试通过。
- 默认态：正式页面共识别 8 张商品卡；计算样式仅有 `#ebebeb` 的 1px 内框，没有外投影。
- Hover：第二张图透明度从 0 切换为 1、第一张图从 1 切换为 0；第二张图使用 `cover` 并覆盖完整卡面。
- 投影过渡：Hover 使用透明度 14% 的轻投影，进入时约 `0.4s`、离开时约 `0.55s`。
- 折扣角标：背景计算值为 `rgb(141, 194, 156)`，对应 `#8dc29c`；无划线价时显示 `Discount`。
- 移动端：在 `390 × 844 px` 视口下保持两列；角标距离左上角 8px、高度 19px，无横向溢出。
- 浏览器控制台：0 条错误或警告。

## 遗留问题

- 商品划线价尚未配置；配置后角标百分比会自动切换为真实计算结果。
