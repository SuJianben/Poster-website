# 2026-08-20 首页 Editorial 商品卡作品留白

## 本次目标

- 在商品图片与卡片边界之间增加可配置留白。
- 让横版、方形和竖版作品都能完整显示，不再被 `cover` 裁切。
- 保留卡片外文案、第二张图 Hover、立体倾斜、阴影和视差效果。

## 修改范围

- `sections/editorial-with-products.liquid`
- `assets/editorial-depth-cards.css`
- `tests/editorial-product-card-layout.test.ps1`

## 新增内容

- 商品图片增加独立 `editorial-depth-card__media` 内层，负责画布留白。
- 主题编辑器新增 `Product card artwork margin` 滑杆，可在 0%–20% 之间调整，默认 9%。
- 回归测试增加内层结构、后台配置、留白变量和 `object-fit: contain` 检查。

## 调整内容

- 商品图片由向外扩张的 `cover` 改为在内层中居中的 `contain`。
- Collection 商品和静态备用卡片共用同一留白结构。
- 留白只作用于图片卡内部，不改变卡片下方的艺术家、标题和价格布局。

## 影响范围

- 仅影响首页两个 Editorial 分区下方的商品卡。
- 不修改商品数据、图片顺序、Hover 第二图来源、产品页、产品系列页或倾斜轮播。

## 自检

- 状态：已完成，并已同步到正式主题。
- Shopify Theme Check：共检查 62 个主题文件，0 个问题。
- 桌面端：共识别 8 张商品卡；卡片尺寸约 `331.75 × 497.63 px`，默认 9% 留白对应左右约 `29.84 px`、上下约 `44.78 px`。
- 横版作品：`Horse Stories` 主图尺寸 `1440 × 1028`、宽高比约 `1.401`，已通过 `object-fit: contain` 完整显示，没有裁图。
- Hover：悬停后主图透明度由 1 切换为 0，第二张图由 0 切换为 1，原切图效果保留。
- 移动端：在 `390 × 844 px` 视口下保持两列；卡片内容位于图片卡下方，无横向溢出，留白比例一致。
- 浏览器控制台：0 条错误或警告。
- 自动化测试：Editorial 商品卡专项测试通过；完整测试集中仅保留既有的自定义海报 SEO 链接断言差异，本次未扩大范围处理。

## 遗留问题

- 无。
