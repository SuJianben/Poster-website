# 2026-07-17

## 目标

将首页 Banner 调整为参考图库式沉浸轮播，同时沿用主题现有 Figma 素材。

## 修改范围

- `sections/hero-poster.liquid`
- `assets/gallery-hero.css`
- `assets/gallery-hero.js`
- `assets/header-adaptive.js`
- `templates/index.json`

## 调整内容

- 以全屏大图、左侧内容和底部缩略图导航重建 Banner。
- 加入前后切换、缩略图切换和键盘左右方向键支持。
- Banner 切换后重新计算 Header 前景色，确保导航在不同图像上可读。
- 使用主题内已有 Figma 素材作为默认 Banner 内容。
- 根据后续验收要求，改为直接移植参考项目的 6 张原图、样式公式和缩略图队列轮换逻辑。

## 影响范围与自检

- 仅影响首页 Hero 分区和 Header 的颜色取样触发时机。
- Theme Check 通过（35 个文件，0 条问题）；线上首页已确认渲染出轮播结构。计数器改由轮播脚本根据实际卡片数量计算，避免模板端显示异常。
