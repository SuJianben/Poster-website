# 2026-07-30 更新记录

## 商品主图真实内框与外框素材试版

- 目标：不再通过 CSS 渐变、边距或阴影模拟内框与外框，改用用户提供的透明图片素材叠加在商品主图上。
- 修改范围：`sections/main-product.liquid`、`assets/product-showcase.css`、`assets/product-showcase.js`；新增 `assets/ps-passepartout-white.png`、`assets/ps-passepartout-pink.png`、`assets/ps-frame-oak.png`。
- 调整内容：白色/粉色 Passepartout 与橡木外框均为实际 PNG 覆盖图；海报图按对应透明窗口的比例定位。Size 下方保留无内框、白色内框、粉色内框和无框、橡木框选择。用户提供的两个 WK 文件内容相同，因此本次只提供一种橡木外框。
- 影响范围：仅商品详情页主图预览与对应选择项；不改 Shopify 变体、价格、库存或加购逻辑。
- 自检：`node --check`、产品详情结构测试和 Theme Check 均通过（40 files, 0 offenses）；已推送至 `mytest-test-two.myshopify.com` 在线主题 `Poster-website/main`（ID `157726048445`）。浏览器进入 `/collections/all` 时站点返回“无法加载网站”，因此未能完成在线视觉截图复核；代码与素材尺寸、透明通道和叠图比例已本地校验。

## 撤回图片叠图试版

- 原因：实际商品图和透明框洞口的比例组合后，内框在与外框组合时出现图层错位。
- 调整：按需求撤回图片叠图，恢复为此前 CSS 内框/外框预览；页面不再引用新增的透明框素材。
- 自检：脚本语法检查、产品详情结构测试和 Theme Check 均通过（40 files, 0 offenses）；已重新推送至 `mytest-test-two.myshopify.com` 在线主题。

## 商品主图区预留外框画布

- 目标：让海报不再决定主图区高度，为后续外框向外延伸保留稳定空间。
- 调整：桌面主图区改为固定高度画布（`520px` 至 `680px`）；海报在画布内居中显示，最大宽高限制为画布的 `78%`。移动端继续保留原本自适应主图尺寸与滑动逻辑。
- 修改范围：`assets/product-showcase.css`、`assets/product-showcase.js`。
- 自检：脚本语法检查、产品详情结构测试和 Theme Check 均通过（40 files, 0 offenses）；已推送至 `mytest-test-two.myshopify.com` 在线主题。

## 商品主图区固定高度

- 调整：按确认的桌面效果，`.ps-main-media` 固定为 `620px` 高；主图本身的尺寸计算不改，在容器中保持居中。
- 影响范围：仅桌面商品主图区；移动端的媒体规则会覆盖为自适应高度。
- 自检：Theme Check 通过（40 files, 0 offenses）；已推送至 `mytest-test-two.myshopify.com` 在线主题。

## 外框向外延伸

- 调整：外框不再通过内部 `padding` 挤压海报图。选择橡木或黑色外框时，外框以海报当前边界为基准向四周延伸 `18px`，海报本身尺寸保持不变；内框逻辑不改。
- 修改范围：`assets/product-showcase.css`。
- 自检：Theme Check 通过（40 files, 0 offenses）；已推送至 `mytest-test-two.myshopify.com` 在线主题。

## 外框最终占位扩张修复

- 根因：上版将外框绝对定位在海报外侧，视觉出现外框但承载海报的布局盒并未扩张，无法形成外框后的真实最终占位。
- 修复：外框状态下的 `.ps-art-preview` 改为 `content-box`；JavaScript 保持写入原海报宽高，CSS 再于内容盒四边增加 `18px`。因此海报仍为原尺寸，最终外框占位宽高各增加 `36px`。
- 自检：Theme Check 通过（40 files, 0 offenses）；真实商品页实测无框海报为 `365×547px`，选择 Oak frame 后海报仍为 `365×547px`，最终外框占位为 `401×583px`，符合外框向外延伸的验收标准；已推送至在线主题。

## 外框木纹材质

- 新增：`assets/ps-frame-oak-light.webp` 与 `assets/ps-frame-oak-dark.webp`，均为 `100×100px`、约 `2.4KB` 的可循环平铺木纹资源。
- 调整：Oak frame 使用浅色木纹，Black frame 使用深色木纹；两者均只作为扩张外框区域的 CSS 背景平铺，不影响海报内容尺寸或外框占位逻辑。
- 修改范围：`sections/main-product.liquid` 负责将主题资源 URL 注入 CSS 变量，`assets/product-showcase.css` 负责外框材质平铺。
- 自检：Theme Check 通过（40 files, 0 offenses）；真实商品页验证两种框型均成功请求对应 WebP 资源，海报保持 `365×547px`，外框占位保持 `401×583px`；已推送至在线主题。

## 外框材质层次优化

- 原因：初版仅将 `100×100px` 木纹直接重复平铺，重复节奏明显，四边纹理方向一致，视觉上像贴图而非实体画框。
- 调整：生成镜像拼接的 `400×400px` 横向/竖向纹理；顶边与底边使用横向木纹、左右边使用竖向木纹；补充内沿暗槽、上下边压边与外投影。
- 新增：`assets/ps-frame-oak-light-vertical.webp`、`assets/ps-frame-oak-light-horizontal.webp`、`assets/ps-frame-oak-dark-vertical.webp`、`assets/ps-frame-oak-dark-horizontal.webp`。
- 自检：Theme Check 通过（40 files, 0 offenses）；真实商品页选择 Oak frame 后确认横向顶/底边、竖向侧边、内沿阴影均已渲染，海报与外框占位尺寸不变；已推送至在线主题。

## 外框 45° 拼角

- 调整：顶部、底部木纹边通过 `clip-path` 裁成 45° 斜切；左右边保留在斜切后的三角区域内，四角形成与实体画框一致的拼接缝。
- 修改范围：`assets/product-showcase.css`。
- 自检：Theme Check 通过（40 files, 0 offenses）；真实商品页确认 Oak frame 状态下四条边均保持渲染，顶部裁切规则为 45° 斜角，未覆盖海报内容；已推送至在线主题。

## Oak 外框改用原始画框九宫格

- 目标：以用户提供的 `WK_2.png` 的真实木框、黑色外沿、内槽与拼角替代 CSS 平铺纹理，保留原图中的实际框体粗细。
- 新增：`assets/ps-frame-oak-border.png`，从原图透明内容区精确裁出 `622×860px` 的边框资产。
- 调整：Oak frame 改用 CSS `border-image` 九宫格拉伸，四角保持原始画框细节，四条边仅沿自身方向延展；海报仍是承载盒内容尺寸，外框在其外侧额外增加 `22px`，不会缩小海报。Black frame 逻辑未改。
- 影响范围：仅商品详情页选择 Oak frame 后的主图预览。
- 自检：Theme Check 通过；已在在线商品页选择 Oak frame 验证，原海报保持 `365×547px`，最终含框占位为 `409×591px`，外框由原图的四角、木纹、黑色内槽真实渲染。

## 撤回 Oak 画框源图，恢复可扩展 CSS 画框

- 原因：用户确认不使用 `WK_2` 作为画框图片。九宫格虽能保持海报尺寸，但原图的边缘比例与任意产品图不一致，视觉会显得框与画面脱离。
- 调整：Oak frame 恢复为纯 CSS 的可扩展结构：可循环木纹仅用于材质，外层木框、45°拼角、黑色内槽、浅色内沿和投影均由 CSS 控制；主图不受比例约束。
- 修改范围：`assets/product-showcase.css`、`sections/main-product.liquid`。
- 自检：Theme Check 通过；已在在线商品页点选 Oak frame 截图复核，框体三层结构与参考图一致，主图无裁切或错位。

## 去除 Oak 画框的额外内圈

- 原因：Oak frame 内侧额外叠加的黑色 `border + box-shadow` 形成了不需要的矩形边框。
- 调整：仅移除 `.ps-art-preview__frame` 在 Oak 状态下的边框与阴影，木框材质、拼角、产品图尺寸和其他框型不变。
- 自检：Theme Check 通过；在线页面计算样式确认内层为 `border: 0`、`box-shadow: none`。
# 2026-07-30

## Oak 外框细节

- 调整 Oak 外框厚度为 `10px`，仅影响 Oak frame 选项。
- 上下横框同步使用 `10px` 斜切拼角，并增加 `0.3px` 轻微柔化，减弱四角的尖锐感。
- 自检：Theme Check 通过；在线商品页确认 Oak 状态下外框 `padding` 为 `10px`、拼角高度为 `10px`、柔化滤镜为 `blur(0.3px)`。

## Black 外框细节

- 调整 Black frame 外框厚度为 `10px`，并与 Oak frame 保持相同的 `10px` 斜切拼角和 `0.3px` 拼角柔化。
