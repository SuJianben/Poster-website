# 2026-07-22 更新记录

## 系列页 hover 对齐 Society6

- 目标：取消集合页图片悬停缩放，改为与 Society6 参考页一致的商品卡底部黑色 Quick view 上滑反馈。
- 修改范围：`sections/main-collection.liquid`、`assets/collection-showcase-interactions.css`。
- 调整内容：子系列图片与 fallback 商品卡图片在 hover 时保持尺寸不变；每张 fallback 商品卡加入纯展示的 Quick view 层，hover 或键盘聚焦卡片时从图片底部上滑至可见位置。
- 影响范围：仅集合页 fallback 商品卡与子系列卡的 hover 外观；不改变筛选、排序、卡片跳转或 Header。
- 自检：Theme Check 通过（0 errors）；主题 `Poster-website/main`（ID `198653444177`）已发布。通过 `preview_theme_id` 新会话验证每张卡均输出 Quick view 节点，悬停 Paper Bird 后该层 `transform` 为 `translateY(0)`、背景为黑色、文字为白色。

## 子系列卡 hover 修正

- 结论修正：子系列卡在参考页 hover 时应保留轻微图片放大，先前将该效果误判为无缩放。
- 调整内容：移除覆盖规则，恢复模板原有的 `scale(1.035)`；fallback 商品卡仍保持不缩放，并继续显示黑色 Quick view 上滑效果。
- 验证计划：发布后在真实预览页分别悬停子系列卡和商品卡，检查两类交互互不影响。

## 系列页筛选布局补齐

- 目标：将集合页 fallback 的筛选区信息密度、数量列与展开结构对齐 Society6 的集合页布局。
- 新增：`snippets/collection-filter-list.liquid` 统一渲染筛选选项、数量和额外选项；每个分组默认展开前 10 项，Show All 可展开其余项目。
- 调整：系列页 fallback 选项补齐为与参考页一致的分组密度，筛选项右侧增加数量列并保留现有前端筛选和排序逻辑。
- 自检：Theme Check 通过（37 files, 0 offenses）；主题已发布。Shopify 店铺在发布后两次页面重载均返回 HTTP 503，真实页面截图验证暂受平台响应阻塞。

## 系列页几何对齐

- 对照方法：在 1920px 桌面尺寸下直接测量 Society6 与 Shopify 的实际 DOM 边界。
- 调整：内容容器由 1290px 收为 1280px；集合区顶部留白、故事区到商品区的节奏，以及排序栏到商品网格的距离按参考页测量值对齐。
- 最终验证：参考页容器 `x=315 / width=1280`、首张商品卡 `x=568.3 / y=793.9`；Shopify 公开链接实测为 `x=312.5 / width=1280`、`x=566.5 / y=793.6`。主题检查 0 问题，线上 HTML 已同步新筛选内容和布局样式。

## 子系列与筛选区可见间距修正

- 根因：子系列图片使用了 `height: 100%`，在 230px 网格行内被拉伸为 `200 × 230px`，使标题行溢出到卡片容器外，导致标题与 `REFINE` 视觉上几乎相连。
- 调整：桌面端改为参考页同样的 `200 × 200px` 图片加 30px 标题行；筛选区下移 25px，并把工具栏留白校正为 24px，使标题到 `REFINE` 与 `REFINE` 到商品首行两段可见距离分别保持为参考图的约 57px、43px。
- 待验证：发布后以线上实际截图复核“标题文字底边 → REFINE”与“REFINE → 商品首行”两段间距。
