# 2026-07-21

## 深度视差卡片占位比例
- 目标：恢复 Nordic Calm 与 Cinematic Nights 分区在改造前的海报卡片占位大小。
- 修改范围：`assets/editorial-depth-cards.css`。
- 调整内容：桌面端恢复四列等宽与 2:3 海报比例；移动端保持两列 2:3 比例，并使用原网格的 20px 间距。
- 影响范围：仅深度视差卡片尺寸与网格，不影响图片、文案、3D 交互或 Header。
- 自检：Theme Check 通过；线上验证为四列等宽、2:3 比例。

## 分区背景色
- 目标：恢复深度视差卡片分区的白色背景，并保留 Handpicked drops 原有浅灰背景。
- 修改范围：`assets/editorial-depth-cards.css`。
- 调整内容：将 `.editorial-depth-shell` 从参考源码带入的棕灰色 `#bcaea8` 改为白色 `#fff`；Handpicked drops 已使用原有 `#ececec`，未重复覆盖。
- 影响范围：仅两个目标分区的背景呈现，不影响卡片、图片、交互或 Header。
- 自检：待主题检查与线上颜色确认。

## Banner 缩略图与控制层
- 目标：缩略图尺寸减半、保持固定顺序，且使 Banner 文案与切换按钮互不影响。
- 修改范围：`sections/hero-poster.liquid`、`assets/gallery-hero.css`、`assets/gallery-hero.js`。
- 调整内容：桌面端缩略图缩放比例由 `0.11` 改为 `0.055`，移动端由 `0.16` 改为 `0.08`；缩略图顺序按模板原始顺序重算，不再随切换轮换；选中项明确恢复为正常亮度；按钮移入独立的绝对定位层。
- 影响范围：仅首页 Gallery hero，不影响其他轮播、Header 或页面内容。
- 自检：Theme Check 通过；线上点击切换与截图确认缩略图已缩小、顺序固定，选中主图正常亮度，按钮与文案为独立层。

## Banner 三槽版本回退
- 目标：回退“三槽缩略图”版本，恢复主图之外显示多张缩略图的布局。
- 回退范围：撤销三槽轨道及其样式、脚本改动，恢复至 `8b423c1` 所对应的 Banner 功能状态。
- 保留内容：缩略图减半、固定顺序、选中项正常亮度，以及文案与按钮独立定位。
- 自检：Theme Check 通过；线上确认三槽轨道不存在，主图之外恢复 5 张缩略图。

## Banner 三张图片
- 目标：Banner 主图与缩略图均只保留三张图片。
- 修改范围：`sections/hero-poster.liquid`、`assets/gallery-hero.css`、`assets/gallery-hero.js`、`templates/index.json`。
- 调整内容：保留原有顺序的前 3 张（Into the Pines、The High Country、Last Light Offshore）；主题配置与分区上限同步为 3；中间当前缩略图使用模板同款圆角样式，两侧保持模板缩略图和原切换动画。
- 影响范围：仅首页 Gallery hero。
- 自检：Theme Check 通过；线上验证主图数量为 3，初始计数 `01 / 03`，点击后为 `02 / 03`；底部始终显示 3 张，当前中间缩略图随主图同步更新。

## Banner 缩略图固定槽位
- 目标：选中的缩略图留在其原始槽位，不再自动移动至中间；选中态仅取消灰色蒙版和圆角。
- 修改范围：`assets/gallery-hero.css`、`assets/gallery-hero.js`。
- 调整内容：三张图按索引固定在左、中、右三个槽位；选中缩略图为直角、正常亮度，未选中的两张延续模板的圆角和灰色蒙版。
- 影响范围：仅首页 Gallery hero 的缩略图呈现与状态切换，不影响主图动画、文案、按钮或 Header。
- 自检：Theme Check 通过；本机两条浏览器通道均被 `ERR_BLOCKED_BY_CLIENT` 拦截，未能完成测试店铺的线上切换验证。

## Banner 选中缩略图视觉一致性
- 目标：选中状态不改变缩略图的视觉占位，仅以取消灰色蒙版表达高亮。
- 修改范围：`assets/gallery-hero.css`。
- 调整内容：选中项恢复模板同款 `48px` 圆角，与未选中项保持相同尺寸与轮廓，仅亮度保持为正常值。
- 影响范围：仅首页 Gallery hero 缩略图选中态，不影响固定槽位和主图切换动画。
- 自检：待 Theme Check。

## Banner 原版缩略图圆角还原
- 目标：选中缩略图的圆角与参考源码缩放后的视觉效果一致。
- 修改范围：`assets/gallery-hero.css`。
- 调整内容：原版 `48px` 圆角作用于全屏元素后会随缩略图缩放同步缩小；当前选中缩略图改为 `calc(48px * var(--phg-thumb-scale))`，桌面端实际约为 `2.6px`，不再产生过大的圆角观感。
- 影响范围：仅首页 Gallery hero 选中缩略图的圆角，不改变尺寸、固定位置或高亮逻辑。
- 自检：待 Theme Check。

## Banner 选中缩略图尺寸对齐
- 目标：选中缩略图与两侧原版缩略图的视觉宽高、底部基线完全一致。
- 修改范围：`assets/gallery-hero.css`。
- 调整内容：移除选中项错误的 `16:9` 容器比例，改为与原版同样按视口整体缩放的宽高计算：`100vw × 缩放比例` 与 `100vh × 缩放比例`。
- 影响范围：仅首页 Gallery hero 的选中缩略图尺寸；不改固定槽位、圆角或高亮逻辑。
- 自检：待 Theme Check。

## 系列页展示改版
- 目标：参考 Society6 的系列页信息架构，创建可配置的 Shopify 原生集合展示页。
- 修改范围：`sections/main-collection.liquid`、`assets/collection-showcase.css`。
- 调整内容：新增系列标题与引言、横向美学子系列、系列说明与 FAQ、原生集合商品网格；美学卡片和 FAQ 均可在主题编辑器中维护。
- 影响范围：仅集合页，不影响首页、Header、商品页或现有购物流程。
- 自检：Theme Check 通过；使用店铺密码会话请求 `/collections/all`，确认线上已加载 `collection-showcase.css` 并渲染标题、子系列轨道、编辑说明和商品区。Shopify CLI 本地预览需要 Theme Access/Admin token，前台店铺密码不能替代该令牌。
