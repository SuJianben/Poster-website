# 2026-07-21

## 深度视差卡片占位比例
- 目标：恢复 Nordic Calm 与 Cinematic Nights 分区在改造前的海报卡片占位大小。
- 修改范围：`assets/editorial-depth-cards.css`。
- 调整内容：桌面端恢复四列等宽与 2:3 海报比例；移动端保持两列 2:3 比例，并使用原网格的 20px 间距。
- 影响范围：仅深度视差卡片尺寸与网格，不影响图片、文案、3D 交互或 Header。
- 自检：Theme Check 通过；线上验证为四列等宽、2:3 比例。

## 分区背景色
- 目标：恢复深度视差卡片分区的白色背景，并保留 Handpicked drops 原有浅灰背景。
- 修改范围：`assets/editorial-depth-cards.css`。
- 调整内容：将 `.editorial-depth-shell` 从参考源码带入的棕灰色 `#bcaea8` 改为白色 `#fff`；Handpicked drops 已使用原有 `#ececec`，未重复覆盖。
- 影响范围：仅两个目标分区的背景呈现，不影响卡片、图片、交互或 Header。
- 自检：待主题检查与线上颜色确认。

## Banner 缩略图与控制层
- 目标：缩略图尺寸减半、保持固定顺序，且使 Banner 文案与切换按钮互不影响。
- 修改范围：`sections/hero-poster.liquid`、`assets/gallery-hero.css`、`assets/gallery-hero.js`。
- 调整内容：桌面端缩略图缩放比例由 `0.11` 改为 `0.055`，移动端由 `0.16` 改为 `0.08`；缩略图顺序按模板原始顺序重算，不再随切换轮换；选中项明确恢复为正常亮度；按钮移入独立的绝对定位层。
- 影响范围：仅首页 Gallery hero，不影响其他轮播、Header 或页面内容。
- 自检：Theme Check 通过；线上点击切换与截图确认缩略图已缩小、顺序固定，选中主图正常亮度，按钮与文案为独立层。

## Banner 三槽缩略图
- 目标：Banner 仅展示三张缩略图，并让当前图也保留在固定缩略图位置。
- 修改范围：`sections/hero-poster.liquid`、`assets/gallery-hero.css`、`assets/gallery-hero.js`。
- 调整内容：新增独立缩略图轨道，固定显示“上一张 / 当前张 / 下一张”三槽；当前图位于中间、取消灰色蒙版；其余图片隐藏，切换时仅更新三槽内容。
- 影响范围：仅首页 Gallery hero，不影响 Banner 主图、文案、按钮或其他分区。
- 自检：Theme Check 通过；线上验证首次显示“第 6 / 第 1（当前）/ 第 2”，切换后显示“第 1 / 第 2（当前）/ 第 3”，始终仅三张且当前图固定中间。

## Banner 三槽样式与切换动效回退
- 目标：保留三槽规则的同时，恢复 Gallery 模板原有的缩略图外观和主图缩放切换动画。
- 修改范围：`sections/hero-poster.liquid`、`assets/gallery-hero.css`、`assets/gallery-hero.js`。
- 调整内容：两侧缩略图恢复模板的 48px 圆角、70% 亮度与 `.8s` 缩放轨迹；当前项使用同样外观固定在中间；仅相邻两张进入两侧模板缩略图位，避免出现第 4 张。
- 影响范围：仅首页 Gallery hero。
- 自检：Theme Check 通过；线上截图确认三槽均为模板圆角与暗化外观。点击下一张后，在 200ms 处捕获到主图仍为 `scale(.055)` 起始状态，并按原 `.8s` 缩放过渡完成；始终仅显示三张。
