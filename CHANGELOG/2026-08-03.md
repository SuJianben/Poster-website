# 2026-08-03 更新记录

## 商品页：主图与外框画布贴合修正

### 本次目标

- 让外框、内边框与产品主图使用同一个预览画布，避免外框看似未包住图片或右侧留缝。

### 修改范围

- `assets/product-showcase.css`
- `assets/product-showcase.js`
- `sections/main-product.liquid`

### 调整内容

- 保留主图区容器的既有高度，不再由脚本额外拉伸主图区。
- 预览画布根据原图比例生成；主图、内边框、外框均作为同一画布中的绝对定位图层渲染。
- 覆盖旧主图的 `height: auto` 与最大高度限制，确保主图尺寸与预览画布完全一致。
- 外框切换不再使用容器尺寸动画。

### 自检结果

- Shopify Theme Check：40 个文件检查通过，0 个问题。
- 在线商品页 `Chair with Fruit`：主图、预览画布、图层容器均为 `443 × 620`，不再出现此前主图 `443 × 547`、画布 `443 × 620` 的高度不一致。
- 已推送至线上主题 `Poster-website/main`（主题 ID：`157726048445`）。

### 遗留说明

- 现有外框素材适合竖版画布。若后续要让横版或正方形产品也达到同样的真实边框效果，需要分别提供对应比例的透明外框素材。

## 商品预览按参考源码恢复缩放与裁切

### 本次目标

- 修复选择外框后主图从右侧、底部漏出的问题，让主图与框保持在同一张预览画布内。

### 修改范围

- `assets/product-showcase.css`

### 调整内容

- 主图恢复参考源码的 `object-fit: contain` 与 `.75` 缩放；选择内框时使用 `.57` 缩放。
- 预览画布及组合容器统一使用 `overflow: hidden`，不再允许主图超出画布。
- 内框、外框素材只在各自图层内铺满同一预览画布，消除素材透明边与产品图片比例差异带来的右侧、底部缝隙。
- 未修改主图容器高度、右侧信息区或其他页面分区。

### 自检结果

- Shopify Theme Check：40 个文件检查通过，0 个问题。
- 真实商品页选择 Black frame 后验证：主图画布为 `225 × 315`，外框图层同样为 `225 × 315`，且外框不再超出组合容器。
- 已推送到线上主题 `Poster-website/main`（主题 ID：`157726048445`）。

### 遗留说明

- 黑框仍沿用现有木框素材并通过滤镜变黑；本次未改变素材来源。

## 左侧商品预览按参考源码移植

### 本次目标

- 将商品详情页左侧主图区域改为参考站点的画布图层结构，不再继续沿用旧的单图尺寸限制逻辑。

### 修改范围

- `sections/main-product.liquid`
- `assets/product-showcase.css`
- `assets/product-showcase.js`
- 新增参考预览素材：`assets/ps-print-frame-nature-oak.png`、`assets/ps-print-frame-dark-oak.png`、`assets/ps-print-passepartout-white.png`

### 调整内容

- 使用 `data-pdp-print-preview`、`data-pdp-canvas`、`data-pdp-print-art` 等参考源码同类节点，统一主图、内框和外框的预览画布。
- 主预览默认使用 Shopify 商品媒体最后一张图片作为干净画面；选择外框或内框后仍在同一画布切换图层。
- 移除旧 `.ps-main-media__image` 的 `max-height` 限制，避免主图比外框矮导致错位、漏缝。
- 桌面端补充参考源码的视口高度尺寸逻辑；未改动右侧购买信息区。

### 自检结果

- Shopify Theme Check：40 个文件检查通过，0 个问题。
- `node --check assets/product-showcase.js` 通过。
- 线上商品页已验证：默认状态读取最后一张干净图；选择 Oak frame 后外框图层与预览画布保持同一 `225 × 225` 几何区域，主图 `max-height` 为 `none`，不再被旧规则压缩。
- 已推送至线上主题 `Poster-website/main`（主题 ID：`157726048445`）。

### 备份

- 修改前文件备份：`D:\CODEX项目\海报站\backups\2026-08-03-source-left-before`

### 遗留说明

- 当前内置浏览器测试面板宽度约为 958px，未触发源码的 `min-width: 990px` 桌面媒体查询；真实桌面宽度达到 990px 时会启用对应的视口高度规则。

## 左侧商品主图箭头切换闪烁修复

### 本次目标

- 修复点击左侧主图左右箭头时，当前图片先闪空再切换的问题。

### 修改范围

- `assets/product-showcase.js`

### 调整内容

- 移除切图前强制设置 `opacity: 0` 以及固定 150ms 延迟。
- 切换时先用临时图片对象预加载目标媒体，加载完成后再提交主图地址，加载期间保持当前图可见。
- 增加切换序号，快速连续点击时只允许最后一次切换提交，避免旧请求覆盖新状态。

### 自检结果

- `node --check assets/product-showcase.js` 通过。
- Shopify Theme Check：40 个文件检查通过，0 个问题。
- 已推送至 `mytest-test-two.myshopify.com` 的线上主题 `Poster-website/main`（主题 ID：`157726048445`）。
- 真实浏览器点击右箭头和左箭头验证：按钮正常响应；切换前、切换后 50ms、切换后 250ms 主图 `opacity` 均保持 `1`，当前媒体正常变化，未再出现透明闪烁。

### 备份

- 修改前文件备份：`D:\CODEX项目\海报站\backups\2026-08-03-arrow-switch-before`

## Frame 下拉框按参考源码样式调整

### 本次目标

- 将 Frame 下拉框改成参考站点的源码式展开样式。

### 修改范围

- `sections/main-product.liquid`
- `assets/product-showcase.css`
- `assets/product-showcase.js`

### 调整内容

- 触发区改为 `+ ADD FRAME` 与 `FROM €35,00` 的左右布局。
- 展开列表改为圆点、名称、尺寸和价格的横向排列，并加入源码式滚动条。
- 保留现有 Frame 选择、主图预览和点击外部关闭逻辑；选择 Oak/Black 后触发区同步显示名称和价格。
- 修正展开后重复出现两行 `ADD FRAME` 的问题，列表只保留一行顶部触发区。

### 自检结果

- `node --check assets/product-showcase.js` 通过。
- Shopify Theme Check：40 个文件检查通过，0 个问题。
- 已推送至 `mytest-test-two.myshopify.com` 的线上主题 `Poster-website/main`（主题 ID：`157726048445`）。
- 真实打开下拉框确认：只显示一行 `ADD FRAME`，3 个选项正常排列；点击 Oak frame 后下拉框关闭，触发区同步为 `OAK FRAME / €45,00`，主图预览状态为 `oak`。

### 当前版本备份

- `D:\CODEX项目\海报站备份\2026-08-03-frame-dropdown-style-after`

## Passepartout 下拉框按 Frame 样式统一

### 本次目标

- 将商品详情页上方的 Passepartout（内框）选择器改为与 Frame（外框）一致的源码式下拉交互。

### 修改范围

- `sections/main-product.liquid`
- `assets/product-showcase.css`
- `assets/product-showcase.js`

### 调整内容

- 内框触发区采用 `+ ADD PASSEPARTOUT` 与 `FROM €15,00` 的左右布局。
- 展开列表沿用外框的圆点、名称、尺寸、价格和滚动区域样式，并保留 No/White/Black 三个内框选项。
- 复用通用选择器逻辑：选择后自动关闭下拉、同步名称与价格，并更新主图内框状态；点击选择器外部仍可关闭。

### 自检结果

- `node --check assets/product-showcase.js` 通过。
- Shopify Theme Check：40 个文件检查通过，0 个问题。
- 已推送至 `mytest-test-two.myshopify.com` 的线上主题 `Poster-website/main`（主题 ID：`157726048445`）。
- 真实商品页验证：内框下拉正常展开并显示 3 个选项；选择 White passepartout 后触发区显示 `WHITE PASSEPARTOUT / €15,00`，下拉关闭，主图 `data-has-passepartout` 状态更新为 `true`。

### 当前版本备份

- `D:\CODEX项目\海报站备份\2026-08-03-mat-dropdown-after`
