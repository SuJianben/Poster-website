# 2026-07-28 更新记录

## 商品页图库轨道与购买区交互

- 目标：依据 Dotcom Canvas 录屏补齐商品页缩略图轨道、主图切换、右侧选项状态、框型提示和加购按钮悬停反馈。
- 修改范围：`sections/main-product.liquid`、`assets/product-showcase.css`、`assets/product-showcase.js`、`tests/product-showcase-accordion.test.ps1`。
- 调整内容：新增缩略图轨道上下控制与主图左右控制；缩略图选择仍维持原位置和黑色描边；主图左右按钮循环切换已有媒体。右侧材质、尺寸和框型选项补齐 `aria-pressed` 状态及标题同步；框型按钮悬停或键盘聚焦时显示名称提示；加购按钮悬停改为白底黑边反白样式。
- 影响范围：仅商品页桌面图库和购买区交互。移动端仍使用横向缩略图列表，轨道上下控制会隐藏。
- 自检：结构测试通过；Theme Check 通过（39 files, 0 offenses）；主题已推送至 `Poster-website/main`（ID `198653444177`）。真实商品页验证中，主图下一张由第一张媒体切至第二张；White frame 点击后标题、选中项与 `aria-pressed` 均为 White frame/true；浏览器控制台无 error。当前测试商品只有两张图片，缩略图轨道 `scrollHeight` 与 `clientHeight` 同为 547px，因无溢出而不会产生可见滚动，这属于预期边界。

## 主图切换箭头悬停动效

- 调整：主图两侧箭头默认完全隐藏；鼠标进入主图容器后显示。箭头按钮默认白底黑箭头，悬停或键盘聚焦时黑色背景通过从左到右的渐变位移铺开，箭头同步切为白色。
- 修改范围：`assets/product-showcase.css`、`tests/product-showcase-accordion.test.ps1`。
- 自检：结构测试通过；Theme Check 通过（39 files, 0 offenses）；主题已重新推送至 `Poster-website/main`（ID `198653444177`）。真实页面默认状态下箭头 `opacity=0` 且不可点击；鼠标进入主图区后变为 `opacity=1` 且可点击。

## 商品页统一箭头与加购扫入动画

- 参考验证：真实体验 Dotcom Canvas 商品页，确认其图库上下控制采用白色圆形容器和细线 chevron；加购按钮为黑底白字的反色过渡。
- 调整：缩略图上下控制与主图左右控制统一替换为细线 SVG chevron，桌面尺寸统一为 `48px` 圆形；所有箭头 hover/focus 统一使用黑色从左向右扫入、图标转白的动效。
- 调整：加购按钮改为默认黑底白字，hover/focus 时白色背景从左向右扫入、文字转黑；不影响原有 Shopify 表单提交或 loading 文案更新。
- 修改范围：`sections/main-product.liquid`、`assets/product-showcase.css`、`tests/product-showcase-accordion.test.ps1`。
- 自检：结构测试通过；Theme Check 通过（39 files, 0 offenses）；已推送到 `Poster-website/main`（ID `198653444177`）。真实页面确认 4 个 chevron 均已渲染，主图箭头与缩略图轨道箭头尺寸均为 `48 × 48px`，主图箭头默认 `opacity=0`，加购扫入层默认 `scaleX(0)`。

## 缩略图轨道紧凑排列修复

- 根因：缩略图轨道具有固定高度，但内部 CSS Grid 未指定内容对齐方式；当前商品仅有两张图时，自动网格行会拉伸并分散填满轨道高度。
- 修复：缩略图网格改为固定 `72px` 行高并使用 `align-content: start`，保持 `10px` 间距；超过可视高度时仍使用原有轨道滚动。
- 修改范围：`assets/product-showcase.css`、`tests/product-showcase-accordion.test.ps1`。
- 自检：结构测试与 Theme Check 均通过（39 files, 0 offenses）；主题已发布。真实页面两张缩略图首尾间距实测为 `10px`，不再被拉开。

## 商品页底部信息标签页

- 目标：将商品页底部信息区调整为 Dotcom Canvas 参考图中的横向标签页结构，并保留真实切换行为。
- 调整：新增 Description、Fair Pricing、Artist Information、Material & Workmanship、Shipping and Returns、Security 六个标签；桌面标签字号为 `28px`，未选中项为浅灰色，选中项为黑色并显示 `2px` 底部下划线。内容区改为与页面同宽的版心，不再使用原先较窄的说明容器。
- 交互：每次仅显示当前标签对应内容，点击后同步更新 `aria-selected`、面板可见状态及选中下划线；移动端保留同一标签逻辑，并支持横向浏览标签。
- 修改范围：`sections/main-product.liquid`、`assets/product-showcase.css`、`assets/product-showcase.js`、`tests/product-showcase-accordion.test.ps1`。
- 自检：结构测试与 Theme Check 均通过（39 files, 0 offenses）；主题已推送至 `Poster-website/main`（ID `198653444177`）。真实桌面商品页验证中，六个标签均正常渲染；点击 Fair Pricing 后其面板成为唯一可见面板，Description 面板已隐藏。

## 底部标签页版心与滚动条修复

- 根因：标签区初版使用了过宽的 `1800px` 版心；同时 `overflow-x: auto` 在活动下划线越过底线时触发了浏览器的纵向滚动条。
- 修复：标签区改为与商品详情 `.ps-shell` 一致的最大 `1280px` 版心；桌面端收紧标签间距并同时隐藏两个方向的溢出，手机端保留无可见滚动条的横向滑动。
- 自检：结构测试与 Theme Check 均通过（39 files, 0 offenses）；已推送主题。真实桌面页实测标签区与商品详情区宽度均为 `1280px`，`scrollWidth` 与 `clientWidth` 均为 `1280px`，六项标签完整可见且无纵向滚动条。

## 商品页参考站版心尺寸对齐

- 参考验证：在 `1920px` 桌面视口实测 Dotcom Canvas 商品页，主区为 `1430px`，左侧媒体区为 `822px`，右侧信息区为 `558px`，中间间距约 `50px`。
- 调整：商品详情主区与底部标签区统一改为最大 `1430px`；右侧信息列调整为最大 `558px`，让上方商品区和下方标签区使用同一视觉宽度。
- 自检：结构测试与 Theme Check 均通过（39 files, 0 offenses）；已推送主题。测试站同视口实测主区/左媒体/右信息/底部标签区分别为 `1430 / 822 / 558 / 1430px`，与参考站一致，标签区仍无滚动条。

## 全局购物车侧边栏

- 目标：新增与参考样式一致的右侧购物车抽屉，并让商品页加购后留在当前页面直接打开抽屉。
- 新增：`snippets/cart-drawer.liquid` 负责结构，`assets/cart-drawer.css` 负责抽屉视觉，`assets/cart-drawer.js` 负责读取 Shopify 真实购物车、数量调整、删除、打开与关闭；新增 `tests/cart-drawer.test.ps1` 做结构回归检查。
- 调整：`layout/theme.liquid` 全站加载购物车资源，`sections/header.liquid` 的 Cart 入口改为打开抽屉，`assets/product-showcase.js` 使用 Shopify `/cart/add.js` 加购并把真实返回数据交给抽屉渲染。
- 修复：购物车脚本原先可能早于页面底部抽屉结构执行，导致初始化退出；现改为 DOM 完成后初始化，保证顶部 Cart 和商品加购共用同一逻辑。
- 影响范围：全站 Cart 入口与商品页加购；不涉及结账、订单或支付逻辑。
- 自检：结构测试通过；Theme Check 通过（40 files, 0 offenses）；已推送至 `Poster-website/main`（ID `198653444177`）。真实商品页验证：点击 Add to cart 后仍停留商品页，抽屉打开且渲染真实商品；抽屉宽度为 `498px`；点击数量加一后数量由 3 更新为 4、小计同步更新为 `€319.96`。按需求，本次未实现多件优惠/免邮阶梯进度条。

## 购物车抽屉即时打开修复

- 根因：加购入口此前需先等待 `/cart/add.js` 和后续 `/cart.js` 都返回后才调用抽屉打开逻辑。在网络响应较慢时，视觉上会像先跳转到购物车、随后才弹出。
- 修复：商品页 Add to cart 与头部 Cart 入口现在会在点击瞬间阻止默认跳转并打开抽屉加载态；真实购物车数据返回后再替换为商品、价格和数量内容。
- 修改范围：`assets/cart-drawer.js`、`assets/product-showcase.js`、`tests/cart-drawer.test.ps1`。
- 自检：脚本语法检查、结构测试与 Theme Check 均通过（40 files, 0 offenses）；已推送至 `Poster-website/main`（ID `198653444177`）。真实商品页验证：点击后 300ms 内抽屉已打开且仍停留在商品 URL；数据回填后抽屉仍保持打开并显示商品。头部 Cart 入口也已验证为即时打开且不跳转。

## 购物车结账按钮扫入动效

- 调整：Secure Checkout 按钮复用商品页 Add to cart 的反色动效：默认黑底白字，hover 或键盘聚焦时白色背景从左向右扫入，锁图标与文字同步变为黑色。
- 修改范围：`assets/cart-drawer.css`、`assets/cart-drawer.js`、`tests/cart-drawer.test.ps1`。
- 自检：脚本语法检查、结构测试与 Theme Check 均通过（40 files, 0 offenses）；已推送至 `Poster-website/main`（ID `198653444177`）。真实购物车抽屉验证中，结账按钮文案层和图标层均正确位于扫入背景上方，悬停后背景与文字反色均已生效。

## 购物车主页面版式重做

- 目标：按提供截图重建 `/cart` 桌面版：面包屑、左侧商品清单、右侧结算摘要及下方 Recently viewed products 横向商品区；按要求不包含优惠进度条。
- 新增：`assets/cart-page.css` 为购物车页面专属样式，`assets/cart-page.js` 仅处理最近浏览商品区的左右滑动，`tests/cart-page.test.ps1` 提供结构回归检查。
- 调整：`sections/main-cart.liquid` 改用 Shopify 原生 cart form、数量更新和 checkout 提交字段，确保商品、价格、数量和结算金额来自真实购物车；推荐商品通过主题编辑器的 collection 设置提供，未选择时使用 all collection 回退。
- 影响范围：仅 `/cart` 页面；侧边购物车抽屉、商品页与结账逻辑不改动。
- 自检：脚本语法检查与结构测试通过；Theme Check 通过（40 files, 0 offenses）；已推送至 `Poster-website/main`（ID `198653444177`）。真实桌面购物车页确认：含 1 个商品行、总数量 7；主区为 `1430px`，左侧商品区 `969px`、右侧结算区 `405px`，页面无横向溢出；推荐区正确渲染 6 张卡，右箭头可向右滚动。

## 购物车与 Header 重叠修复

- 根因：全站 Header 为首页 Banner 采用 `margin-bottom: -72px` 的覆盖式布局，购物车没有补回这个高度，导致面包屑和标题与 Header 发生重叠。
- 修复：仅在 `.cp-cart` 作用域补回 `72px` 顶部空间，桌面和移动端一致；Header 与其他页面均不受影响。
- 自检：结构测试与 Theme Check 均通过（40 files, 0 offenses）；已推送主题。真实桌面购物车页实测 Header 底部为 `y=73px`，面包屑从 `y=73px` 开始、标题从 `y=154px` 开始，二者不再重叠。

## 购物车原地数量交互

- 参考验证：Dotcom Canvas 的数量加减为 Ajax 原地更新，不刷新 `/cart`，数量、行总计、订单总计和 Header 购物车数量同步变化。
- 调整：购物车页的加、减、删除改用 Shopify `/cart/change.js`；当前行在请求期间显示短暂加载态，成功后仅更新对应数值，不打开侧边购物车。
- 自检：脚本语法检查、结构测试与 Theme Check 通过（40 files, 0 offenses）；已推送主题。真实购物车中数量由 7 增至 8 后总计变为 `€639.92`，减回后数量、Header 数量和总计均恢复为 `7 / 7 / €559.93`，页面 URL 未改变。
