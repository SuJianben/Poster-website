# 2026-07-31 更新记录

## 本次目标

- 将新导入的 The Poster Club 参考产品按内容类型建立可复用分类集合。

## 修改范围

- Shopify 后台集合数据。
- 未修改主题代码。

## 新增内容

- 新增 7 个按产品类型自动收录的系列集合：
  - Abstract Posters
  - Floral Posters
  - Landscape Posters
  - Figurative Posters
  - Still Life Posters
  - Urban Posters
  - Time Series Posters
- 为 7 个新系列集合补充集合封面图，图片来源为各集合内代表性产品主图。

## 调整内容

- 每个集合使用 `TYPE equals ...` 自动规则收录对应产品。
- 已发布到 Online Store 渠道。

## 影响范围

- 后续系列页、首页集合选择器、产品筛选可直接使用这些真实集合。
- 未删除或改动旧集合。

## 自检结果

- Abstract Posters：34 个产品。
- Floral Posters：14 个产品。
- Landscape Posters：11 个产品。
- Figurative Posters：18 个产品。
- Still Life Posters：13 个产品。
- Urban Posters：5 个产品。
- Time Series Posters：5 个产品。
- 合计 100 个产品，数量与当前导入产品总数一致。
- 7 个集合均已读取到集合封面图 URL 与 alt 文案。

## 遗留问题

- 旧的 Movie / Music / Anime 等集合暂未处理，避免影响当前页面引用。

---

## 产品详情页主图切换修复

### 本次目标

- 修复产品详情页点击缩略图或左右箭头后，主图视觉上不变化的问题。

### 修改范围

- `sections/main-product.liquid`
- `assets/product-showcase.js`

### 调整内容

- 缩略图按钮补充当前媒体对应的响应式图片 `srcset` 数据。
- 主图切换逻辑从只更新 `src` 改为同步更新 `srcset` 与 `src`。

### 影响范围

- 仅影响产品详情页主图媒体切换。
- 不调整产品信息区、变体、外框、加购、首页或系列页样式。

### 自检结果

- 本地产品详情结构测试通过。
- `shopify theme check` 通过，40 个文件 0 offense。
- 已推送到 `mytest-test-two.myshopify.com` 在线主题 `Poster-website/main`（ID `157726048445`）。
- 真实打开产品 `berit-mogensen-lopez-improvise`，点击第 2 个缩略图和右箭头后，主图 `currentSrc/src/srcset` 均确认切换。

### 遗留问题

- 无。

---

## 产品详情页框状态改为按单张媒体锁定

### 本次目标

- 让自定义的框/内边框效果只跟随当前这张主图，不再让整个媒体组共享同一框状态。

### 修改范围

- `assets/product-showcase.js`
- `sections/main-product.liquid`

### 调整内容

- 为每张媒体单独保存 frame / mat 预览状态。
- 切换主图时，只恢复当前媒体自己的预览状态。
- 给产品详情页主图脚本加了缓存版本参数，尽量避免浏览器继续吃旧 JS。

### 影响范围

- 仅影响产品详情页主图预览，不影响首页、系列页、购物车和搜索页。

### 自检结果

- 本地 Theme Check 通过。
- 远端主题文件已更新。
- 真实浏览器端仍可能受 Shopify 前台缓存影响，若刷新后仍看到旧状态，需要再等前台缓存刷新。

### 遗留问题

- 线上前台 HTML 仍有旧 script 地址的缓存迹象，我先保留这个记录，后续若仍不刷新再继续追缓存层。

---

## 定制预览固定到最后一张纯净图

### 本次目标

- 选择内边框、外框等自定义选项后，自动切换到产品图库最后一张纯净图进行预览。
- 普通产品图片不再显示定制框效果。

### 修改范围

- `assets/product-showcase.js`

### 调整内容

- 将自定义预览状态的目标媒体固定为图库最后一个媒体按钮。
- 选择定制选项时，若当前不在最后一张，先切换到最后一张再应用效果。
- 切换回其他产品图片时，预览状态自动恢复为无框，避免每张图都被套框。

### 影响范围

- 仅影响产品详情页主图的内边框、外框预览逻辑。
- 不改变 Shopify 变体、购物车和产品图片数据。

### 自检结果

- `shopify theme check` 通过，40 个文件 0 offense。
- 已推送在线主题 `Poster-website/main`（ID `157726048445`）。

---

## 商品主图改为固定三层预览结构

### 本次目标

- 按 The Poster Club 参考源码，取消主图的百分比偏移和外框放大计算。
- 让主图、内框、外框始终在同一个预览盒子内叠加，消除不同商品比例下的边缘缝隙和外框撑高。

### 修改范围

- `sections/main-product.liquid`
- `assets/product-showcase.css`
- `assets/product-showcase.js`

### 调整内容

- Liquid 改为明确的主图层、内框图片层、外框图片层，三层共享同一 composition 容器。
- CSS 移除 `left/top/width/height` 的硬编码补偿；内框通过主图居中缩放展示，外框直接覆盖在同尺寸预览盒子上。
- JS 只根据当前主图比例计算预览盒子大小，不再因选外框而改变预览盒子尺寸、位移或下方详情间距；保留“选择定制项自动跳到最后一张纯净图”的原有逻辑。

### 自检

- `shopify theme check` 通过，40 个文件、0 个 offense。
- 已在线验证商品 `Chair with Fruit`：选择 White passepartout 后自动切至最后一张纯净图，主图缩放为 `0.57`；预览盒保持 `390 × 547px`，主图区保持 `620px`。
- 再选择 Oak frame 与 Black frame 后，外框图层均正常显示，预览盒和主图区尺寸均未变化，不再出现外框撑高、飞出或以百分比偏移补缝的情况。
- 已推送在线主题 `Poster-website/main`（ID `157726048445`）。

---

## 内框黑色选项视觉修正

### 本次目标

- 开始处理内框（Passepartout）展示，保持现有主图收缩比例。

### 修改范围

- `assets/product-showcase.css`

### 调整内容

- 白色内框继续使用原素材。
- 黑色内框对透明内框素材增加黑色材质滤镜，避免黑色选项仍显示为白色。
- 外框的 `left: 15px` 对齐设置保持不变。

### 自检结果

- `shopify theme check` 通过，40 个文件 0 offense。
- 已推送在线主题 `Poster-website/main`（ID `157726048445`）。

---

## 校准外框主图左侧偏移

### 本次目标

- 修复外框状态下主图向左偏移、导致右侧出现视觉缝隙的问题。

### 修改范围

- `assets/product-showcase.css`

### 调整内容

- 将 Oak/Black 外框状态的主图左偏移固定为 `15px`，与实际外框开口对齐。
- 未改动图片宽高、主图区高度、内框及其它产品选项。

### 自检结果

- `shopify theme check` 通过，40 个文件 0 offense。
- 已推送在线主题 `Poster-website/main`（ID `157726048445`）。

---

## 修复外框右侧漏缝

### 本次目标

- 修复不同产品比例下，外框透明开口与主图之间出现细缝的问题。

### 修改范围

- `assets/product-showcase.css`

### 调整内容

- 仅在“外框 + 无内框”状态下让主图按外框开口盒子完整铺满，避免 `object-fit: contain` 因比例差异自动留下透明边。
- 内框、产品详情布局、620px 主图区高度及其它状态保持不变。

### 自检结果

- `shopify theme check` 通过，40 个文件 0 offense。
- 已推送在线主题 `Poster-website/main`（ID `157726048445`）。
- 代码层未发现其它受影响状态；浏览器自动化截图在当前环境因 Chrome headless 启动超时，已保留前一次真实商品页截图作为基线。

---

## 外框改为紧贴式图片叠加

### 本次目标

- 外框保持原有 620px 主图区，不撑大容器、不播放外框过渡动画。

### 修改范围

- `sections/main-product.liquid`
- `assets/product-showcase.css`
- `assets/product-showcase.js`

### 调整内容

- 外框素材改用 `ps-frame-oak-border.png`，去除大块透明留白，只保留紧贴外缘的边框图层。
- 外框选择时主图区始终保持 620px，组合尺寸不再参与页面布局；详情区仅保留避免遮挡所需的最小间距。
- 移除外框组合的宽高过渡动画，选中后直接贴合显示。
- 微调外框垂直定位，使其顶部与主图区边缘对齐，避免向上飞出。
- 将主图在外框透明开口内扩大约 0.8%，覆盖素材内缘阴影，消除视觉漏缝。
- 进一步扩大主图覆盖到 93%×95%，压住边框内侧阴影，按 `Chair with Fruit` 最后一张纯净图实测无可见漏缝。
- 使用真实 `Chair with Fruit` 商品的最后一张纯净图复核，确认第 1 张 Oak 场景图不会被重复叠框，纯净图状态下四边已密封贴合。

### 自检结果

- 真实线上页面验证：主图区保持 `620px`，Oak 外框组合约 `478×651px`，详情区从其下方正常排列，未覆盖。
- `shopify theme check` 通过，40 个文件 0 offense。
- 已推送在线主题 `Poster-website/main`（ID `157726048445`）。

---

## 修复外框组合溢出详情区

### 本次目标

- 解决外框组合超出 620px 主图区、覆盖下方 Product details 的问题。

### 修改范围

- `assets/product-showcase.js`

### 调整内容

- 选中外框时，主图区高度按外框组合的实际高度展开；取消外框时恢复 620px。
- 主图尺寸计算保持不变，只有外层媒体区域增高，不再让外框“飞”到折叠详情区。

### 自检结果

- 真实线上商品页验证：选 Oak frame 后主图区高度为 `824px`，外框组合完整位于主图区内，详情区从主图区底部继续排列。
- `shopify theme check` 通过，40 个文件 0 offense。
- 已推送在线主题 `Poster-website/main`（ID `157726048445`）。
- 线上 section rendering API 已返回带 `psv=single-preview-20260731` 的最新脚本引用；完整产品 HTML 仍存在 Shopify 前台缓存旧脚本的情况，需以前台缓存刷新后的页面作为最终视觉验收。

### 遗留问题

- 当前完整产品页缓存仍可能加载旧脚本地址，若页面暂未刷新，需等待 Shopify 前台缓存更新后再做最终点击验收。

---

## 产品框与内衬切换为图片素材渲染

### 本次目标

- 在 CSS 框渲染版已备份后，改用透明 PNG 素材叠加到主图上，保证验收时看到的材质与素材一致。

### 修改范围

- `sections/main-product.liquid`
- `assets/product-showcase.css`
- `backups/css-frame-preview-2026-07-31.zip`

### 调整内容

- 主图容器增加透明木框图片图层，Oak 使用 `ps-frame-oak-border.png`。
- Black 使用同一框素材的深色滤镜图层，保持原有选项和交互不变。
- White/Black passepartout 增加透明内衬图片图层，不再依赖原来的 CSS 木纹伪元素。
- 新增图片图层均带固定宽高，避免 Theme Check 的图片尺寸告警。

### 影响范围

- 仅影响产品详情页主图的框与内衬视觉渲染。
- CSS 版已压缩备份，可回退到修改前状态。

### 自检结果

- `shopify theme check` 通过，40 个文件 0 offense。
- 在线图片素材请求返回 200，PNG 素材已在店铺 CDN 可访问。
- 已推送到在线主题 `Poster-website/main`（ID `157726048445`）。

### 遗留问题

- Shopify 商品页 HTML 仍有前台缓存延迟，section 接口与远端主题文件已是新模板，完整页面视觉需缓存刷新后再最终确认。

---

## 修正图片框的内缩与外扩关系

### 本次目标

- 修正图片框与海报没有对齐的问题，恢复“内框收缩、外框延申”的原有视觉逻辑。

### 修改范围

- `sections/main-product.liquid`
- `assets/product-showcase.css`

### 调整内容

- 移除会覆盖整张画面的内衬图片层，改回容器 padding 让海报实际缩进内框。
- 外框图片改为超出主图容器边缘定位，向外扩展，不改变海报自身比例。
- 保留图片素材渲染的木框质感，取消上一版同尺寸叠加造成的黑色遮挡。

### 自检结果

- `shopify theme check` 通过，40 个文件 0 offense。
- 已推送在线主题 `Poster-website/main`（ID `157726048445`）。

### 遗留问题

- Shopify 商品页仍可能短暂读取旧 HTML 缓存，需刷新缓存后检查最终视觉对齐。

---

## 按透明素材开口重新组合主图、内衬与外框

### 本次目标

- 取消 CSS padding 内缩，改为按用户提供的透明图片素材开口渲染。
- 修复外框图片比例错误、边缘错位的问题。

### 修改范围

- `sections/main-product.liquid`
- `assets/product-showcase.css`
- `assets/product-showcase.js`

### 调整内容

- 主图、内衬图、外框图重组到独立 composition 容器。
- 根据 800×1100 素材的透明开口坐标定位主图：内衬直接由其透明洞控制画面缩小。
- 选择外框时，依据外框透明洞尺寸扩大总画布，主图保持原渲染尺寸；外框仅向外延展。
- 删除上一版 111% 外扩定位，消除框体歪斜和白边错位。

### 自检结果

- 已用实际商品最后一张纯净图复核 Oak frame + White passepartout 状态：主图进入内衬开口，外框边缘对齐。
- `shopify theme check` 通过，40 个文件 0 offense。
- 产品详情折叠交互测试通过。
- 已推送在线主题 `Poster-website/main`（ID `157726048445`）。

### 遗留问题

- 无代码层遗留；若前台仍看到旧效果，请等待 Shopify 页面缓存刷新后再刷新页面。

---

## 外框不再缩小画芯

### 本次目标

- 选择外框时保留主图原始渲染尺寸；仅由外侧透明外框扩展总视觉面积。

### 修改范围

- `assets/product-showcase.js`
- `assets/product-showcase.css`

### 调整内容

- 外框组合的尺寸计算改为以无框主图尺寸为基准，透明框画布不再参与主图的限高计算。
- 移除组合容器的 `max-width: 100%` 限制，避免外框总画布被压回主图区宽度、连带缩小透明开口。

### 自检结果

- 已在上线商品页的全新浏览器上下文中对比：无框画芯为 `429 × 600px`；Oak 外框画芯为 `428 × 600px`（浏览器整数像素取整差异），外框组合扩大至 `604 × 824px`。
- 已逐帧对照参考录屏：外框作为画芯外部的独立视觉层出现，不参与画芯缩放。
- `shopify theme check` 通过，40 个文件 0 offense。
- 已推送在线主题 `Poster-website/main`（ID `157726048445`）。
